services:
  - type: web
    name: inspec360
    runtime: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install && pnpm build && cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
